import React from 'react';
import { expect } from 'chai';
import Deskgrid from './DeskGrid';

describe('Spec Deskgrid', function () {
    it('it exists', () => {
        expect(Deskgrid).to.be.ok;
    });
});
